import { Navigation } from 'components/Navigation/Navigation';
import { AppHeader } from './AppBar.styled';

export const AppBar = () => {
    return (
        <AppHeader>
            <Navigation/>
        </AppHeader>
    )
}